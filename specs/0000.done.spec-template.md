# 0000: Spec template

> This file is the canonical template. Copy it when creating a new spec.

## Scope

Which service / directory this task touches.
Example: `ms-editor`, `editor-ui/src/features/Draw`.

## Context

Why this task exists. Link to relevant doc, ADR, or prior spec if applicable.

## Files

List every file that must be created or modified:

- `path/to/file.go` — what changes (add / modify / delete)
- `path/to/other.go` — what changes

## Interface

Expected signatures, struct shapes, or API endpoints after the change.

```go
// example for Go
func (uc *FigureUseCase) GetByDiagramID(ctx context.Context, diagramID uuid.UUID) ([]*domain.Figure, error)
```

## Constraints

Rules Codex must not break:

- Follow the existing error-handling pattern in this file.
- Do not add new dependencies.
- One migration file per DB change.

## Verify

How to confirm the task is done correctly:

- `go build ./...` passes.
- Endpoint returns expected shape (show curl example if relevant).
- ESLint / tsc passes for frontend tasks.
