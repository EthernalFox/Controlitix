#+ Инженер: настроить тренды и отчеты

Как инженер, я хочу видеть тренды по выбранным тегам и формировать базовые отчеты, чтобы анализировать режимы.

Предусловия
- Значения архивируются (`ms-viewer` → PostgreSQL).

Поток
1) Выбрать теги и период в Viewer.
2) Выполнить запрос трендов через `ms-viewer`.
3) Сохранить шаблон отчета (TBA) и экспортировать (CSV/PDF — TBA).

Acceptance Criteria
- Графики отображаются без ошибок; интервалы и downsampling корректны.
- Экспортируемые отчеты содержат требуемые поля и временные метки.

Связанные сущности и API
- История: таблицы исторических рядов `ms-viewer` (TBA в модели данных).
